a = 5
b = 10
print(
    "If we do a>b then we will get this -->",
    a > b,
    "\nIf we do b>a then we will get -->",
    a < b,
    "\nIf we do b==a then we will get -->",
    a == b,
    "\nIf we do a>=b then we will get -->",
    a >= b,
    "\nIf we do a<=b then we will get -->",
    a <= b,
    "\nIf we do a!=b then we will get -->",
    a != b,
)

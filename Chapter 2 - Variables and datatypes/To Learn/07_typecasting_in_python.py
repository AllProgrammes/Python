a = "100"
print(
    "Here a is of ",
    type(a),
    "type \nbut we have to change it to int type so that we can perform the below operations",
)
print("\nChanging the type of a to [int]......")
print("The value of a+1 is ", (int(a) + 1))
